spring.application.name=DHBW_Memory
server.port=8080

# Vaadin: restrict component scanning to our package and disable auto-opening
# a browser tab on startup (we open it manually).
vaadin.allowed-packages=de.dhbw.memory
vaadin.launch-browser=false
logging.level.org.atmosphere=warn
